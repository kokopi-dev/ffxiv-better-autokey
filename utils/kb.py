#!/usr/bin/env python3

class Keyboard:
    def __init__(self):
        self.esc = "{VK_ESCAPE}"
        self.select = "{VK_NUMPAD0}"
        self.up = "{UP}"
        self.down = "{DOWN}"
        self.submenu = "'"
